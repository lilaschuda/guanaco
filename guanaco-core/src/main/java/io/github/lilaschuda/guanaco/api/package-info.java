/**
 * Guanaco's public route-authoring API: {@link io.github.lilaschuda.guanaco.api.Processor},
 * {@link io.github.lilaschuda.guanaco.api.RouteOutcome} and its built-in outcome types.
 *
 * <p>{@code @NullMarked}: everything in this package is non-null by
 * default -- parameters, return types, fields -- unless explicitly
 * annotated {@code @Nullable}. This is read by Kotlin's compiler to give
 * proper nullable/non-null inference at the Java/Kotlin boundary, instead
 * of the ambiguous platform types (`String!`) Kotlin otherwise falls back
 * to for unannotated Java APIs.
 */
@NullMarked
package io.github.lilaschuda.guanaco.api;

import org.jspecify.annotations.NullMarked;
